/**
 * Display name per review source — shared by the reply drafter and every
 * Reviews-tab UI piece that needs one (was declared separately in three
 * places before this consolidation).
 */
export const PLATFORM_LABEL: Record<string, string> = {
  google: 'Google',
  tripadvisor: 'TripAdvisor',
  withlocals: 'Withlocals',
  getyourguide: 'GetYourGuide',
}
